# MCP Server Environment Variables
# Copy this file to .env.mcp and fill in your actual values

# Jenkins MCP Configuration
JENKINS_URL=https://your-jenkins-server.com
JENKINS_USERNAME=your-jenkins-username
JENKINS_PASSWORD=your-jenkins-token-or-password

# GitHub MCP Configuration (if using PAT instead of OAuth)
# GITHUB_TOKEN=your-github-personal-access-token

# Note: For Figma, Playwright, Magic UI, Sequential Thinking, and FastAPI MCP,
# no environment variables are required as they use npx with auto-installation.
